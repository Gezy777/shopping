# Build stage
FROM golang:1.23-alpine AS builder

WORKDIR /app

RUN apk add --no-cache git

ENV GOPROXY=https://goproxy.cn,direct
ENV GO111MODULE=on

COPY . .

RUN mkdir -p /app/output/bin /app/output/conf     && cd app/user     && CGO_ENABLED=0 GOOS=linux go build -o /app/output/bin/user .     && cp -r script /app/output/     && cp -r conf /app/output/

# Runtime stage
FROM alpine:3.19

WORKDIR /app

RUN apk add --no-cache ca-certificates

COPY --from=builder /app/output/bin/user /app/bin/user
COPY --from=builder /app/output/script/bootstrap.sh /app/
COPY --from=builder /app/output/conf /app/conf
RUN chmod +x /app/bootstrap.sh

EXPOSE 8080

ENTRYPOINT ["/app/bootstrap.sh"]
